services:
  runner-agent:
    image: noodlescripter/runner-agent:latest
    container_name: runner-agent
    restart: unless-stopped
    ports:
      - "3000:3000"
    volumes:
      - runner-data:/app/data
    environment:
      - NODE_ENV=production

volumes:
  runner-data:
